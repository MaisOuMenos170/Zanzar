import SwiftUI

struct __FEATURE__View: View {
    @State private var viewModel = __FEATURE__ViewModel()
    @Environment(AppCoordinator.self) private var coordinator

    var body: some View {
        // TODO: build the UI. Extract reusable pieces into Views/Components/.
        // Every string here is a Localizable.xcstrings key, e.g.:
        // Text("__feature__.header.title")
        Text("__feature__.placeholder.title")
    }
}

#Preview {
    __FEATURE__View()
        .environment(AppCoordinator())
}
